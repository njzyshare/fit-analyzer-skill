# 华为转换（铁律 #1–#4）

1. altitude 缩放：字段类型 sint16、单位 0.5m，写入 `round(米*2)`；invalid=`0x8000`（非 65535）；高驰读 `altitude` 算爬升，只写 `enhancedAltitude` 爬升显示 0。
2. 华为爬升翻倍：华为气压计 0.5~1m 噪声，逐点累加爬升约翻倍（实测 80m→162m），是传感器精度限制非编码错误，告知用户即可。
3. 禁去噪：不篡改原始数据，不做去噪/过滤/缩放修改。
4. 计圈距离：用 Haversine 逐点累加，按 1000m 触发切圈（线性插值会偏小 3%）。
