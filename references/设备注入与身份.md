# 设备注入与身份（铁律 #7,#28）

7. 注入佳明：须真实设备身份；`apply` 删原第三方 device_info 留单条佳明；剥离第三方开发者字段（重复 field_description 会被 Connect 拒收）；身份存 `~/.workbuddy/private/garmin_devices.json`。
28. 设备身份从 `~/.workbuddy/private/fit_devices.json` 读，绝不写死；规范只存 manufacturer/product/productName，不含序列号。

rebrand 铁律见 SKILL「设备身份注入」段：高驰源常带 10+ 条 device_info，只换第一条留四不像→用 `inject_coros.py` 全删重嵌。
