# 协议字节与时间戳（铁律 #8,#9）

8. 时间平移：SDK 重编码产物含压缩时间戳消息，时间平移一律用 `shift_time_sdk.mjs`，禁用字节级 `fit_shift_time.py`。
9. 协议字节：回传 COROS 必须 `0x20`，传佳明标准 `0x02`；改之须同时重算 header CRC（字节0–11→12–13）+ file CRC（末2字节），只重算一道 `checkIntegrity` 仍 false；CRC 用 Garmin 半字节表。
