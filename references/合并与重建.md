# 合并与重建（铁律 #5,#6,#10,#11）

5. fit_preview.py 面板 id 用 `psession`/`pfields`/`plaps`，勿用缩写（JS 拼接对不上）。
6. SDK Encoder 重建丢私有消息（gps_metadata、timestamp_correlation 等）；接收端严格校验时需确保 laps/splits/event 存在。
10. 佳明合并改高驰：`fit_merge.py` 对佳明解析失败→用 `merge_fixed.mjs`；段间大时差重定基线（留 5s 暂停）防 106 分钟怪物圈；每 km 圈强制精确 1km 边界。
11. 暂停感知：圈运动时间用相邻 record 距离增量反算瞬时速度 `Δ距/Δ时` 判暂停，不依赖可能缺失的 `speed` 字段；段间停顿默认保留，仅 >60min 或时间戳重叠才压缩；暂停计入 elapsed 不计配速（`totalTimerTime`=运动时间，`totalElapsedTime`=墙钟）。
